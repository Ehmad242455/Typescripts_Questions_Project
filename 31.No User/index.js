let userNames = ["Admin", "Hassan", "Zubair", "Arfan", "Abdullah"];
if (userNames.length === 0) {
    console.log("We need to find some users!");
}
else {
    userNames = [];
    console.log("All user named have been removed " + userNames.length);
}
export {};
